import React, { Fragment } from 'react';
import { LoginDefault } from '../components/features';

const Login = () => {
  return (
    <Fragment>
      <LoginDefault />
    </Fragment>
  );
};

export default Login;
